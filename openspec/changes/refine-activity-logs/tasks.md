## 1. Fix the running-status/highlight color collision

- [x] 1.1 In `crates/agentmon/src/ui.rs`, change `SELECTED_ROW_BG` to a color distinct from every agent-status and test-run-status foreground color (Blue/Running&Started, Gray/Idle, Yellow/NeedsInput, Green/Done&Passed, DarkGray/Stale, Red/Declined&Failed); update its doc comment if the rationale changes.
- [x] 1.2 Add a unit test in `ui.rs` asserting no `status_label_and_style`/`test_run_status_cell_text_and_style` foreground color equals `SELECTED_ROW_BG`, so a future status addition can't reintroduce the collision; verify it passes against the new color and would have failed against the old `Color::Blue`.
- [x] 1.3 Add/extend a `TestBackend` rendering test that selects the (only) row of a project whose agent is "running" and asserts the "running" text and its emoji are actually present in the rendered buffer with a color distinct from the row background - verify it fails against the pre-fix color and passes after.

## 2. Fix the registry's late-event resurrection of a superseded session id

- [x] 2.1 In `crates/agentd/src/registry.rs`, add a bounded record to `RegistryState` of session ids retired by the same-pid dedup (global set capped at `MAX_SUPERSEDED_SESSIONS`, not per-pid - see design.md's revised rationale), populated wherever `upsert` currently removes an old entry for a reused pid.
- [x] 2.2 In `Registry::upsert`, before treating an unseen session id as brand-new, check it against the tombstone record; if it names a session already superseded, drop the event as a no-op (no registry mutation, no returned "is_new" agent creation), returning the pid's current live entry unchanged - verified with a unit test mirroring `a_new_session_id_for_a_tracked_pid_replaces_the_old_entry` but adding a late event for the replaced session id afterward, asserting the registry still shows only the superseding session and is unchanged by the late event.
- [x] 2.3 Add a unit test for the chained case in design.md's "pid has since moved on again" scenario: session A superseded by B, B later superseded by C, then a late event for A arrives - assert the registry still shows only C, unaffected.
- [x] 2.4 In `crates/agentd/src/ingest.rs`, verify (with a test if not already covered) that a dropped late event per 2.2 does not notify or append an activity-log entry, consistent with it being treated as a no-op. (Found and fixed a real gap in the process: `should_notify` unconditionally notifies on a "needs input" status regardless of whether it changed, so returning the pid's live entry unchanged from `upsert` would have silently double-notified/double-logged whenever that live entry happened to be "needs input" - added an explicit `UpsertOutcome::stale_event_ignored` flag so `ingest_event` short-circuits before reaching `should_notify` at all.)
- [x] 2.5 Add an end-to-end test in `crates/agentd/src/server.rs`'s test module reproducing the race at the socket level: report session A on pid P, report session B on pid P (supersedes A), then report a late event for session A on pid P over a fresh connection - assert the snapshot still shows only B.

## 3. Move empty-state placeholders into the table body

- [x] 3.1 In `render_agent_table` (`crates/agentmon/src/ui.rs`), drop the `app.agents.is_empty() && app.test_runs.is_empty()` title branch; render "Agents" as the title unconditionally and draw the "no agents tracked yet" message as gray body text beneath the header when there are no rows, reusing the same technique as the Logs tab's filter-mismatch message.
- [x] 3.2 In `render_logs_tab`, drop the `app.logs.is_empty()` title branch so the title always shows just the sort/filter control hints; draw "no activity logged yet" as gray body text beneath the header when `app.logs` is empty (distinct from the existing "no activity matches the current filter" case, which only applies when the *unfiltered* log is non-empty but the filtered view is).
- [x] 3.3 Update the existing tests that assert the empty-state text appears in the Agents/Logs tab titles (search `ui.rs` for "no agents tracked yet" / "no activity logged yet" in title-focused assertions) to instead assert the title is plain ("Agents" / just the controls hint) and the body carries the gray placeholder text - verify `cargo test -p agentmon` passes. (Also added a new `agents_tab_shows_a_gray_placeholder_in_the_table_body_when_empty` test, since the Agents tab's empty state previously had no dedicated test at all.)

## 4. Documentation

- [x] 4.1 Add a concise README section covering the Logs tab and activity log (what it captures, the 500-entry bound, how to reach it and the project details modal), consistent in tone/length with the existing "How it works" section - verify it renders correctly and doesn't duplicate the existing state-diagram content.

## 5. Verification

- [x] 5.1 Run the full workspace test suite (`cargo test --workspace`) and confirm all tests pass, including the new/updated ones from tasks 1-3. (Also ran `cargo clippy --workspace --all-targets`: clean except one pre-existing doc-formatting warning in `registry.rs` unrelated to this change, confirmed present on `main` before these edits.)
- [x] 5.2 Manually verify against an isolated `HOME`-scoped `agentd`/`agentmon` pair (per this project's manual-testing convention - never the default socket path): start a fresh agent and confirm its "running" status is visible on the initially-selected row; simulate a `/clear`-style pid reuse followed by a deliberately delayed report for the old session id, and confirm no duplicate/flickering row appears; confirm both tabs show the gray empty-state body text with no tracked agents/logs. (Ran the real `agentd`/`agentmon`/`agentmon-report` binaries under `HOME=/tmp/amon-verify-home`, socket separate from the production daemon: empty-state capture showed plain "Agents" title + gray "No agents tracked yet" body text with SGR `38;5;8` = `Color::DarkGray`; a real running agent (pid of a live `sleep` process, so the liveness sweep wouldn't mark it stale) rendered "🔧 running Ns" on the default-selected row with SGR `38;5;4` (Blue) foreground against `48;5;5` (Magenta) background - distinct, confirming visibility; a same-pid supersession (session-2 replacing session-1) followed by a deliberately late `ReportEvent` for session-1 left the daemon's snapshot byte-for-byte unchanged (`last_updated_ms` included) and the TUI showed exactly one row throughout, with no stray log entries. Isolated environment torn down afterward; verified the user's real production `agentd` (separate pid, default socket) was undisturbed throughout.)
